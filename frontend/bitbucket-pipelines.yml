image: node:latest
pipelines:
  default:
    - step:
        script:
          - docker login -u venuzs -p venu@1234
          - docker build -t venuzs/reactjs:v1 .
          - docker push venuzs/reactjs:v1
options:
  docker: true